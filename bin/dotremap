#!/usr/bin/env ruby

lib = File.dirname(__FILE__) + "/../lib"
$:.unshift lib unless $:.include? lib

require "dotremap"

DOTREMAP_PATH = File.expand_path("~/.karabiner")

dotremap = Karabiner.new(DOTREMAP_PATH)
dotremap.apply_configuration
